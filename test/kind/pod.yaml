apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: tenant-a
spec:
  priorityClassName: "preemptible"
  containers:
  - name: cuda-container
    image: busybox:latest
    resources:
      requests:
        cpu: 1
        memory: 1Gi
      limits:
        cpu: 5
        memory: 1Gi
